{
  "name": "pixel-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-manifest": "node scripts/generate-manifest.mjs",
    "dev": "npx --yes serve . -l 8000",
    "watch": "npx --yes chokidar-cli \"assets/art/**/*\" -c \"npm run generate-manifest\" --initial"
  },
  "dependencies": {
    "image-size": "^1.1.1"
  },
  "devDependencies": {
    "eslint": "^10.7.0",
    "globals": "^17.7.0"
  }
}
